# --- video / captures (generated by rov_client.py) ---
*.mp4
*.avi
*.mov
stream_*.mp4
capture_*.csv
.DS_Store
# --- Python ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/

# --- virtualenv (yours is named client/auvc, not a standard venv/ name) ---
client/auvc/
pyvenv.cfg

# --- model weights (large binaries; comment out if you want them tracked) ---
*.pt
*.onnx

# --- YOLO / Roboflow dataset export + training runs ---
YOLO/
*.zip
runs/
train/images/
train/labels/
valid/images/
valid/labels/
test/images/
test/labels/

# --- local config / credentials (per-machine, not shared) ---
.rov_server_creds
mailmap.txt
repo-backup.git/

# --- OS / editor junk ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
